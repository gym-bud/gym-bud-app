# gym-bud-node

## Installation

### Requirements
* MySQL
* node

### Linux / Unix
Run `install.sh`. This sets up mysql, installs npm modules, and copies over hooks.

### Everything else
Do the above manually.
